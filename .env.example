# Optional: enables LLM-based intent parsing, refinement parsing, and richer explanations.
# If omitted or set to "placeholder", the app still runs with deterministic fallbacks.
ANTHROPIC_API_KEY=placeholder

# Frontend expects the backend at this URL unless frontend/src/api.js is changed.
VITE_API_BASE_URL=http://localhost:8000
