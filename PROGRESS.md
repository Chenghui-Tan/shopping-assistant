# PROGRESS — Overnight Capstone Build

Branch: `overnight-capstone-20260430`
Start: 2026-04-30 ~01:45 PDT
Constraint: 8-hour budget, ≥5 iterations.

## Inputs read

- ✅ `docs/ChenghuiTan-Capstone Proposal.docx` — proposal text + 9 embedded images (5 demo screenshots: chat / clarify / grid / why-fits / lifecycle)
- ✅ `Ducuments requirement.docx` — rubric: Intro/BizProblem, Related Work, Data, Models, Results, Conclusions, Contributions (incl. AI), References (IEEE), Appendix; ToC, numbered figures/tables, IEEE citations, professional cover, 10–12 min presentation, LinkedIn post.
- ✅ `report-sample-report/` — 5 sample PDFs (6–10 pages each, ~22–29k chars).
- ✅ `docs/capstone_report.pdf` — current 15-page LaTeX report, decent base, needs polish + IEEE citations + appendix expansion.
- ✅ `docs/capstone_presentation.pptx` — current deck (already generated by `build_ppt.py`).
- ✅ Codebase map: `backend/` (FastAPI + Claude client + session store + questions), `recommendation_Algorithem/` (deterministic engine with category-specific scoring), `frontend/` (React 3-stage flow), `data/clean/products_clean.json` (100 products).

## Architectural decision

Keep existing codebase. The recommendation engine, FastAPI routes, Claude client and React shell are already aligned with the proposal's 5-layer model. **Two scenes are missing in the UI**: Scene 4 ("Why we recommend this" detail page) and Scene 5 (lifecycle / daily-use). Will add as new components instead of rewriting from scratch — meets the demo screenshots without throwing away ~3 weeks of working code.

## 5-Scene flow target (from proposal screenshots)

1. **Frustration / Empathic chat** — User vents pain in chat bubble; assistant responds with empathy. Header `Kitchen Assistant — Always here to help`. Step 1 of 5.
2. **Needs Clarification** — Multi-question chip form ("Cook alone or family?", "What features?"). Header `Let me understand your needs better`.
3. **Recommendations grid** — 12-product grid with sale badges, ratings, tags. Header `Recommended for you`.
4. **Explainable recommendation detail** — "Why we recommend this": bulleted reasons, trade-offs callout, transparency banner. Back button.
5. **Lifecycle / Daily-use** — `Your Assistant, Every Day — Beyond shopping`. Today's schedule, today's menu, video tile, meal-planning + family-hub + entertainment cards.

## Plan

| Iter | Goal | Output |
|------|------|--------|
| 1 (now) | Plan + read inputs + map architecture | PROGRESS.md (this file) |
| 2 | Frontend revamp to 5-scene flow + lifecycle/why-fits views | New components, Stage 4 & 5, themed CSS, scene captions |
| 3 | Demo scenarios + verify backend works | `docs/demo_scenarios.md` + smoke test of 3 paths |
| 4 | Rewrite report to A-level | `docs/capstone_report_v2.docx` + `.pdf` |
| 5 | Slide deck v2 | `docs/capstone_presentation_v2.pptx` |
| 6+ | Adversarial critique → fix lowest-scoring goal | `CRITIQUE.md`, follow-up commits |
| Final | HANDOFF.md | summary + open items |

## Second iteration loop (2026-05-02 — A through E)

Triggered by user request to keep iterating + fill missing data. All five
iterations committed individually; see git log.

| Iter | Subject | Outcome |
|---|---|---|
| A | Backfill missing data (72 delivery, 100 description, 2 rating) | All 100 rows complete; provenance flags added; engine finds richer rule matches |
| B | Personalise lifecycle | 10 distinct dashboards across 3 categories — verified |
| C | Preference continuity | Diff card per refine turn; 3-turn history; HUMAN_KEY copy |
| D | Contextual replies + chip pre-fill | 8 use-case-aware replies; reverse-mapped chip pre-fill from inferred prefs |
| E | Save-for-later persistence + report v3 | Toggle endpoint, per-session list, counter pill, heart UI; report v3 = 21 pp / 41k chars; ppt v3 |

After each iteration: backend tests run (27/27), frontend build verified
clean (~220 KB JS).

## Decisions made (no user available)

- **Adapt, don't rebuild**: existing code is solid; I'm adding scenes, not throwing it out. Will note this in CRITIQUE.md.
- **Three categories**: keep the existing three (smart_display, water_bottle, kitchen_organizer). The proposal's headline demo is "Kitchen Assistant" but the codebase already supports all three; demo scenarios will use one per category to satisfy the "3 demo scenarios" goal.
- **No Claude API calls during dev**: the API key in `.env` may be a placeholder. Will mock LLM paths if needed for smoke tests.
- **Report tooling**: extend the existing `build_report_v2.py` / `build_report_v3.py` into `build_report_v4.py` rather than starting from scratch — matches user's existing pattern.
- **PPT tooling**: extend `build_ppt_v3.py` into `build_ppt_v4.py`.
